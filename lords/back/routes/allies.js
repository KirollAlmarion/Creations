const router = require("express").Router();

module.exports = (app) => {    

    //router.get('/', app.controllers.allies.getAll);

    //router.post('/create', app.controllers.allies.create);

    //router.post('/getById', app.controllers.allies.getById);

    //router.post('/update', app.controllers.allies.update);

    //router.post('/delete', app.controllers.allies.remove);

    return router;
};